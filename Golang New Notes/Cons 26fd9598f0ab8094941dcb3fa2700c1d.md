# Cons

```markdown
** Syntax **
const name = value
const name type = value

** Example **
const PI = 3.14
const Msg string = "Hello"

// Multiple
const A, B = 10, 20

// iota (auto increment)
const (
  Sun = iota // 0
  Mon        // 1
  Tue        // 2
)

// write simple main function which is using this
```